# agents.txt

A minimal, machine-readable file that tells AI agents what a website can do.

## Why

Agents currently scrape HTML, guess workflows, and hallucinate capabilities.

`agents.txt` flips that:

> The site declares capabilities, the agent adapts behavior.

## Usage

Place a file at:
https://yoursite.com/agents.txt
